#### How to run

```bash
$ cd rest-api
$ mvn clean test 
// to generate report in \target\site\allure-maven-plugin
$ mvn allure:report  
// or run the below goal to start allure server with generated and deployed report 
$ mvn allure:serve 
```

